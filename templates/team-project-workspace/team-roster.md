# Team Roster

> 每位成员至少要提供固定字段。
> `project folder path` 建议直接写成 `members/<member-id>/`。
> `canonical profile path` 可留空；留空时，系统应退化为“先问卷建画像，再派任务”。

| member id | display name | canonical profile path (optional) | project folder path | declared role | expected contribution | current status |
|---|---|---|---|---|---|---|
| member-template | Example Member |  | `members/member-template/` | role name | 用一句话描述该成员这次预期贡献 | active |
